import React, { useContext } from 'react'
import { Context } from '..' 

function NavBar() {
    const {user} = useContext(Context)
    return (
        <div>
            
        </div>
    )
}

export default NavBar